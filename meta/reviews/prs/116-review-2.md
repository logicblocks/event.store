---
type: pr-review
id: "116-review-2"
title: "Add optional metadata bag to events"
date: "2026-06-26T14:13:08+00:00"
author: "bivav-ebury"
producer: review-pr
status: complete
target: "pr:116"
relates_to: ["pr-review:116"]
reviewer: "bivav-ebury"
verdict: "APPROVE"
lenses: ["correctness", "test-coverage", "database", "compatibility", "documentation"]
review_number: 2
pr_number: 116
tags: ["metadata", "events", "schema-migration", "re-review"]
last_updated: "2026-06-26T14:13:08+00:00"
last_updated_by: "bivav-ebury"
schema_version: 1
---

## Code Review (Re-review): #116 - Add optional metadata bag to events

**Verdict:** APPROVE

This is a re-review of PR #116 after the findings from review #1 ([[116-review-1]]) were addressed. All ten original findings are confirmed **RESOLVED** with evidence, and the full build is green (lint passed, 0 type errors, 1689 unit + 134 integration + 3 component tests pass). One new minor documentation inconsistency surfaced and was fixed during this pass. No outstanding blocking or major issues remain.

### Resolution Summary

| # | Original finding (severity) | Status |
|---|------|--------|
| 1 | Builder falsy-`or` coalescing discards `{}`/falsy values — both builders, both fields (major) | ✅ RESOLVED — explicit `is not None` checks + `{}` regression tests |
| 2 | Postgres unit test asserted metadata by type only (major) | ✅ RESOLVED — now asserts `params[6].obj`/`params[15].obj` by value with explicit metadata |
| 3 | `None`/omitted metadata never round-tripped through Postgres (major) | ✅ RESOLVED — `test_stores_event_with_omitted_metadata_as_none` + empty-`{}` case added to shared `StreamSaveCases` (runs against both adapters) |
| 4 | `metadata` field type didn't reflect `None` (minor) | ✅ RESOLVED — declared `Metadata \| None` on both events + builders; type-check clean |
| 5 | `StoredEvent.metadata` required-arg breaking change not in changelog (major) | ✅ RESOLVED — dedicated "Breaking changes" section |
| 6 | `serialise()` gains `metadata` key not documented (major) | ✅ RESOLVED — noted under "Changed" |
| 7 | Migration backfilled `'{}'` vs app `None` (minor, cross-cutting) | ✅ RESOLVED — backfill now `'null'::jsonb`, with rationale |
| 8 | No `lock_timeout`/deploy-ordering guidance (minor) | ✅ RESOLVED — `SET lock_timeout` + migrate-before-deploy note added |
| 9 | No rollback note (suggestion) | ✅ RESOLVED — non-destructive rollback documented |
| 10 | `publish` docstrings + README example + plan dangling ref (minor/suggestion) | ✅ RESOLVED — both docstrings updated, README example added, plan reference removed |

### New Finding (fixed during this pass)

- 🔵 **Documentation** — The plan Overview still claimed metadata "defaults to `{}`", contradicting the code (`None`) and the rest of the same plan. Fixed: now reads "defaults to `None` / JSON `null`, not `{}`".

### Residual (non-blocking) suggestions

- 🔵 **Test Coverage** — Non-mapping metadata (list/scalar) is permitted by the `Metadata = JsonValue` type but never round-trip tested. Optional: add one shared case with a non-object metadata value. (Payload has the same pre-existing mapping-centric coverage, so risk is low.)
- 🔵 **Database** — The `None`→JSON-`null` *storage encoding* is asserted only indirectly (via the application-level round-trip). Optional: assert the raw column reads back as JSONB `'null'` in the Postgres integration suite.
- 🔵 **Compatibility** — `pyproject.toml` still reads `0.1.11`; given two documented breaking changes, confirm the release bumps to at least `0.2.0` (not a patch). Likely handled by release tooling, not the fragment.

### Verification

`mise run` targets all green (run individually to avoid the incremental-cache `test:report` combine artifact):
- `lint:fix` — All checks passed
- `format:fix` — 218 files left unchanged
- `types:check` — 0 errors
- `test:unit` — 1689 passed
- `test:integration` — 134 passed
- `test:component` — 3 passed

---
*Review generated by /accelerator:review-pr*
